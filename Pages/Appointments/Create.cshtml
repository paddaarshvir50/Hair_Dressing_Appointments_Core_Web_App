@page
@model Hair_Dressing_Appointments_Core_Web_App.Pages.Appointments.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Appointment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Appointment.ClientId" class="control-label"></label>
                <select asp-for="Appointment.ClientId" class ="form-control" asp-items="ViewBag.ClientId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Appointment.HairDresserId" class="control-label"></label>
                <select asp-for="Appointment.HairDresserId" class ="form-control" asp-items="ViewBag.HairDresserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Appointment.HairDressingOptionId" class="control-label"></label>
                <select asp-for="Appointment.HairDressingOptionId" class ="form-control" asp-items="ViewBag.HairDressingOptionId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
